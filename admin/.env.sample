# API (Fastify) — include /api/v1
VITE_API_URL=http://localhost:5000/api/v1

# Public menu site base (no trailing slash). Used for “copy link” on Profile — same URL you put in Instagram.
# Local Astro: http://localhost:4321
VITE_PUBLIC_MENU_BASE_URL=http://localhost:4321
